#include "components.hpp"

int main() {
std::string = "TEST NAME";
int month = 1, day , year
    std::string ret = (new TRANS_HANDLE(name, new Date(month, day, year), amount))->addTrans(env->GetStringUTFChars(TRANS_JSON_PARM, nullptr));
 std::cout << ret
}